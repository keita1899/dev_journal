services:
  backend:
    build:
      context: ./backend
      target: development

    volumes:
      - ./backend:/backend
      - bundle_data:/usr/local/bundle

    working_dir: /backend

    ports:
      - 3300:3000

    depends_on:
      - db

    tty: true

    stdin_open: true

    env_file:
      - ./.env
      - ./backend/.env

  db:
    image: postgres:16

    env_file:
      - ./.env

    ports:
      - '5436:5432'

    volumes:
      - 'postgres_data:/var/lib/postgresql/data'

volumes:
  postgres_data:
  bundle_data: